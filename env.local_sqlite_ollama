# ============================================================================
# LOCAL MODE (FASTEST) - SQLite (Feith mock) + Ollama (LLM)
# ============================================================================
# Use this when you want a guaranteed local run with no external dependencies.
# This is the exact mode you already validated successfully.
#
# How to use:
#   cp env.local_sqlite_ollama .env
#   python3 validate_single.py DOC-00001
#   python3 wednesday_demo.py

# ----------------------------------------------------------------------------
# FEITH (Source)
# ----------------------------------------------------------------------------
# Force the script to use the built-in SQLite Feith mock DB (feith_mock.db)
FEITH_FORCE_SQLITE=true

# Optional schema (leave blank for SQLite)
FEITH_DB_SCHEMA=

# Safety: do not write back to Feith in local mode
FEITH_WRITEBACK_ENABLED=false

# ----------------------------------------------------------------------------
# TARGET (Destination / Staging)
# ----------------------------------------------------------------------------
# Store the transformed records locally
TARGET_DB_URL=sqlite:///migration_staging.db

# ----------------------------------------------------------------------------
# LLM (Local)
# ----------------------------------------------------------------------------
# Ollama default local endpoint
LLM_API_URL=http://localhost:11434/api/generate
LLM_MODEL=llama3

# ----------------------------------------------------------------------------
# Processing
# ----------------------------------------------------------------------------
BATCH_SIZE=50
CONFIDENCE_THRESHOLD=0.7
MAX_WORKERS=4

# ----------------------------------------------------------------------------
# Runtime
# ----------------------------------------------------------------------------
LOG_LEVEL=INFO
MAX_RETRIES=3
LLM_TIMEOUT=180
