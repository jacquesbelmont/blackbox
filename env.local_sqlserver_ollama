# ============================================================================
# LOCAL MODE (REALISTIC) - SQL Server (Feith simulated) + Ollama (LLM)
# ============================================================================
# Use this when you want to simulate a real Feith DB connection locally.
# Recommended on macOS: Azure SQL Edge Docker container.
#
# How to use:
#   1) Start SQL Server container (Azure SQL Edge)
#   2) Create DB: feith
#   3) Import samples: python3 load_feith_samples.py --input ./feith_samples.csv --db-url "$FEITH_DB_URL"
#   4) cp env.local_sqlserver_ollama .env
#   5) python3 validate_single.py DOC-00001

# ----------------------------------------------------------------------------
# FEITH (Source)
# ----------------------------------------------------------------------------
# IMPORTANT: disable the SQLite mock
FEITH_FORCE_SQLITE=false

# Use FEITH_DB_URL to point to SQL Server via SQLAlchemy.
# NOTE: requires pyodbc + ODBC Driver 18 installed on your machine.
# If you use a different ODBC driver version, update the driver name accordingly.
FEITH_DB_URL=mssql+pyodbc://sa:YourStrong!Passw0rd@localhost:1433/feith?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

# Optional schema (usually blank for SQL Server dbo default)
FEITH_DB_SCHEMA=

# Safety: keep read-only
FEITH_WRITEBACK_ENABLED=false

# ----------------------------------------------------------------------------
# TARGET (Destination / Staging)
# ----------------------------------------------------------------------------
TARGET_DB_URL=sqlite:///migration_staging.db

# ----------------------------------------------------------------------------
# LLM (Local)
# ----------------------------------------------------------------------------
LLM_API_URL=http://localhost:11434/api/generate
LLM_MODEL=llama3

# ----------------------------------------------------------------------------
# Processing
# ----------------------------------------------------------------------------
BATCH_SIZE=50
CONFIDENCE_THRESHOLD=0.7
MAX_WORKERS=4

# ----------------------------------------------------------------------------
# Runtime
# ----------------------------------------------------------------------------
LOG_LEVEL=INFO
MAX_RETRIES=3
LLM_TIMEOUT=180
